# Spec: Palindrome Checker

Implement a Python function `is_palindrome(s: str) -> bool` that returns `True` if the input
string is a palindrome (ignoring case and non-alphanumeric characters), `False` otherwise.

Deliver it as a new file `palindrome.py` at the root of your worktree, plus automated tests that
verify it.
