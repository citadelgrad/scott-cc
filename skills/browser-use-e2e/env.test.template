# =============================================================================
# browser-use E2E Test Credentials
# =============================================================================
# Copy this file to .env.test in your project root (git-ignored)
#
# Naming convention: SERVICENAME_USER, SERVICENAME_PASS, SERVICENAME_DOMAIN
#
# The LLM never sees real credentials - only placeholders like "github_user"
# that get injected directly into form fields by browser-use.
# =============================================================================

# --- GitHub ---
# GITHUB_USER=your-username
# GITHUB_PASS=your-password-or-token

# --- GitLab ---
# GITLAB_USER=your-username
# GITLAB_PASS=your-password

# --- Google/Gmail ---
# Use App Password if 2FA is enabled: https://myaccount.google.com/apppasswords
# GMAIL_EMAIL=you@gmail.com
# GMAIL_PASS=your-app-password

# --- Custom Application ---
# For apps not in the default domain map, specify the domain explicitly
# MYAPP_USER=admin
# MYAPP_PASS=secretpassword
# MYAPP_DOMAIN=https://app.example.com

# --- Another Custom Service ---
# STAGING_USER=testuser
# STAGING_PASS=testpass
# STAGING_DOMAIN=https://staging.myproduct.com

# =============================================================================
# Usage in test tasks:
#
#   agent = Agent(
#       task='Log in with github_user and github_pass',
#       sensitive_data=build_sensitive_data(),  # from conftest.py
#       use_vision=False,  # Important: prevents credential leaks
#   )
#
# =============================================================================
